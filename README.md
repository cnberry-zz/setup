# setup
Linux setup files for vim, cscope, etc

# Install
Make sure to add:

```bash
# My custom stuff goes in .bashrc.berry
if [ -f ~/.bashrc.berry ]; then
    . ~/.bashrc.berry
fi
```

to your ~/.bashrc
